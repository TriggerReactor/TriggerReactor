function blockname(args){
    w = args[0];
    x = args[1];
    y = args[2];
    z = args[3];
    if (w == null) {
        return null;
    }
    world = Bukkit.getWorld(w);
    bn = world.getBlockAt(x,y,z).getType().name().replace("_", " ").toLowerCase();
    return bn;
}
